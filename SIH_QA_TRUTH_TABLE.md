# SIH 2026 — AI/ML & Decision Intelligence Q&A Truth Table

**Problem Statement:** SIH26059 — *AI-Enabled Antarctic Sea-Ice, Iceberg Trajectory, and Navigation Decision Support System*  
**Evaluation Standard:** Adversarial Red-Team Defense against AI/ML, Geospatial, Maritime, and Software Reviewers.  

This truth table documents the exact, code-verifiable facts of the system. **No marketing inflation. No fabricated accuracy. Transparent engineering limitations.**

---

| # | Judge Question | Exact Truthful Answer | Evidence / Code Symbol | Limitation / Disclosure |
|---|---|---|---|---|
| **1** | **Which AI models are used?** | Three models: (1) `RandomForestRegressor` for monthly Sea-Ice Concentration (SIC) change forecasting; (2) `RandomForestRegressor` for iceberg trajectory displacement prediction; (3) `RegularizedRandomForestClassifier` for Sentinel-1 SAR & Sentinel-2 optical sea-ice/water/iceberg texture classification. | [`sea_ice/train.py`](file:///d:/SIH/backend/src/sea_ice/train.py#L73-L116)<br>[`iceberg/train.py`](file:///d:/SIH/backend/src/iceberg/train.py#L73-L98)<br>[`sentinel/train.py`](file:///d:/SIH/backend/src/sentinel/train.py#L45-L107) | Models are scikit-learn tree ensembles, not deep transformers or neural networks. Selected for robustness under sparse polar observational data. |
| **2** | **Which datasets train them?** | (1) **NOAA/NSIDC CDR V4 (G02202)** monthly polar sea-ice NetCDF; (2) **BYU/NIC Antarctic Iceberg Database** (95,696 tracked trajectory steps across 180+ named targets); (3) **Copernicus Sentinel-1 SAR GeoTIFFs** (15 calibrated polar scenes) + Sentinel-2 optical band ratios. | [`data/raw/sea_ice/real_cdr_series_18m.nc`](file:///d:/SIH/backend/data/raw/sea_ice/real_cdr_series_18m.nc)<br>[`data/raw/iceberg/`](file:///d:/SIH/backend/data/raw/iceberg/)<br>[`data/raw/sentinel/real_s1_scenes/`](file:///d:/SIH/backend/data/raw/sentinel/real_s1_scenes/) | NOAA CDR dataset used is monthly temporal resolution. Iceberg database has variable reporting intervals (12h–72h). |
| **3** | **Why Random Forest instead of Deep Learning / LSTMs?** | Polar satellite records suffer from cloud cover, radar shadow, missing passes, and small sample regimes ($N \approx 28,000$ cells). Random Forest prevents gradient exploding, trains deterministically, enforces bounded outputs ($0.0 \le \text{SIC} \le 1.0$), and enables zero-inference-delay ($<2\text{ ms}$) in operational pathfinding loops. | [`sea_ice/train.py:L73`](file:///d:/SIH/backend/src/sea_ice/train.py#L73)<br>[`iceberg/train.py:L80`](file:///d:/SIH/backend/src/iceberg/train.py#L80) | Deep recurrent networks (ConvLSTM/U-Net) could model higher spatial resolution but require GPU clusters and risk severe hallucination when input rasters have missing data. |
| **4** | **What is SIC?** | Sea Ice Concentration: the fraction of an ocean grid cell covered by sea ice. Normalized strictly from $0.0$ (open ocean water) to $1.0$ (100% solid consolidated pack ice). IMO POLARIS uses SIC categories: Open Water ($<15\%$), Very Open Drift ($15\text{--}30\%$), Open Pack ($40\text{--}60\%$), Close Pack ($70\text{--}80\%$), Compact Pack ($>90\%$). | [`sea_ice/features.py:L118`](file:///d:/SIH/backend/src/sea_ice/features.py#L118)<br>[`optimization/polar_routing_engine.py:L257`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L257) | Historical datasets store percentages ($0\text{--}100$) or fractions ($0.0\text{--}1.0$). The engine normalizes internally to $0.0\text{--}1.0$ to ensure unit consistency. |
| **5** | **What is AIS?** | Automatic Identification System: VHF maritime transponder broadcast carrying vessel MMSI, IMO, coordinates, SOG, COG, heading, and navigational status. In high-latitude Antarctica, terrestrial AIS stations are virtually non-existent; satellite AIS coverage has significant latency gaps. | [`services/ais_service.py:L1-L15`](file:///d:/SIH/backend/services/ais_service.py#L1-L15) | When live terrestrial AIS is unavailable in polar ice, the system explicitly badges telemetry as `● DETERMINISTIC DEMO VOYAGE` rather than faking live satellite data. |
| **6** | **How are icebergs predicted?** | A hybrid physics-coupled kinematic + ML residual pipeline: (1) Copernicus Marine surface ocean currents ($u_o, v_o$) coupled at 70% deep keel drag + 30% vessel inertial drift; (2) Southern Hemisphere Coriolis turning ($-0.10^\circ/\text{h}$ counter-clockwise); (3) Blended with BYU/NIC Random Forest displacement predictions ($25\%$ weight). | [`iceberg/trajectory_service.py:L81-L150`](file:///d:/SIH/backend/src/iceberg/trajectory_service.py#L81-L150) | Not a 3D Navier-Stokes numerical fluid-structure simulation. Keel shape and sail area are approximated from BYU major/minor axis metrics. |
| **7** | **How does weather affect routing?** | Surface wind vectors (speed & relative heading) and significant wave heights from Copernicus Marine / GFS are evaluated at every waypoint. Headwinds and heavy sea states increase hydrodynamic resistance, elevate engine load, reduce Speed Over Ground (SOG), and penalize edge traversal cost in A*. | [`optimization/fuel_model.py:L73-L78`](file:///d:/SIH/backend/src/optimization/fuel_model.py#L73-L78)<br>[`optimization/polar_routing_engine.py:L570-L580`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L570-L580) | Atmospheric forcing uses regional 10m wind and significant wave height grids. Localized katabatic wind gusts from the Antarctic ice sheet are not dynamically modeled. |
| **8** | **How does SIC affect routing?** | High SIC imposes a quadratic exponential cost penalty on grid cells: $\text{Penalty} = 1.0 + (\text{SIC}^2) \times w_{\text{sic}} \times 2.5$. For solid pack ($>90\%$), traversal is blocked for lower ice classes or severely detoured. In open leads ($<15\%$), ice penalty is $1.0$ (calm water). | [`optimization/polar_routing_engine.py:L570`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L570) | The grid resolution is 50 km in wide ocean and 10–20 km near coasts. Leads smaller than the grid cell resolution cannot be individually navigated without onboard ice radar. |
| **9** | **How does A\* work here?** | Discrete 2D A* graph search over an Antarctic Polar Stereographic (EPSG:3031) regular metric grid. 8-directional neighbor connectivity, Euclidean geodesic heuristics with circumpolar curvature bias, dynamic edge weights combining SIC, iceberg CPA, and land mask avoidance. | [`optimization/polar_routing_engine.py:L387-L700`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L387-L700) | A* is an algorithmic graph-search optimizer, **not** AI/ML. Claiming A* as "AI" is technically inaccurate and avoided. |
| **10** | **Why EPSG:3031?** | WGS-84 (EPSG:4326) suffers from extreme meridional convergence near the South Pole (longitude degrees shrink to 0 meters at $-90^\circ$). EPSG:3031 (Antarctic Polar Stereographic) is a conformal metric projection with true scale at $-71^\circ\text{S}$, preserving isotropic distance metrics (meters) across the entire Southern Ocean. | [`optimization/polar_routing_engine.py:L84-L85`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L84-L85) | High distortion above $-60^\circ\text{S}$ (temperate latitudes). Coordinates are re-projected to EPSG:4326 for GeoJSON/MapLibre display. |
| **11** | **Why not WGS84 directly for pathfinding?** | Direct Euclidean distance on lat/lon ($\Delta\text{lat}^2 + \Delta\text{lon}^2$) treats $1^\circ$ longitude at $-70^\circ\text{S}$ ($38\text{ km}$) identically to $1^\circ$ at the equator ($111\text{ km}$), causing severe path distortion, diagonal bias, and false shortcuts across the continental land mass. | [`optimization/polar_routing_engine.py:L83-L105`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L83-L105) | Great-circle equations in spherical coordinates cannot efficiently index uniform 2D grid neighbors for A* closed sets without conformal planar projection. |
| **12** | **How is fuel estimated?** | Physics-based naval architecture: Admiralty cube law for calm water power ($P_{\text{calm}} \propto \Delta^{2/3} v^3$), combined with Lindqvist/Riska empirical ice resistance formulas scaled by Polar Class (PC1–PC7), wind drag, and wave added resistance, multiplied by Specific Fuel Oil Consumption ($185\text{ g/kWh}$). | [`optimization/fuel_model.py:L39-L95`](file:///d:/SIH/backend/src/optimization/fuel_model.py#L39-L95) | **Estimated**, not certified. True fuel burn depends on real-time propeller pitch, hull fouling, trim, and dynamic ice ramming events. |
| **13** | **How are routes ranked?** | Weighted multi-objective optimization across three operational corridors: **Route A (Fastest)** minimizes transit time and distance ($w_{\text{time}}=2.5, w_{\text{sic}}=0.8$); **Route B (Balanced)** optimizes time-risk compromise ($w_{\text{time}}=1.5, w_{\text{sic}}=2.0$); **Route C (Safest)** maximizes safety margin and minimizes ice/iceberg exposure ($w_{\text{sic}}=3.5, w_{\text{ib}}=5.0$). | [`optimization/polar_routing_engine.py:L1190-L1340`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L1190-L1340) | The three options are generated via calibrated weight vectors rather than computing a continuous mathematical Pareto frontier. |
| **14** | **What makes this system "AI-enabled"?** | The AI/ML models provide the **environmental hazard fields** (future sea-ice concentration forecasts, iceberg drift displacement, satellite SAR classification) that feed into the deterministic risk engine and PolarRoutingEngine. AI predicts environmental dynamics; deterministic algorithms compute safe navigational geometry. | [`SIH_TECHNICAL_ARCHITECTURE.md`](file:///d:/SIH/SIH_TECHNICAL_ARCHITECTURE.md#L1-L80) | The routing optimizer itself is deterministic A* graph search, NOT a deep reinforcement learning policy. This preserves predictability and safety certification. |
| **15** | **What is actually simulated?** | (1) Canonical expedition vessel voyages (e.g. *R/V Sagar Nidhi*, *R/V Polarstern*) are simulated with deterministic kinematic progression along approved corridors; (2) Iceberg drift incorporates historical BYU track physics; (3) Missing high-latitude terrestrial AIS is simulated from COMNAP vessel registries. | [`services/ais_service.py:L30-L125`](file:///d:/SIH/backend/services/ais_service.py#L30-L125) | All simulated elements are explicitly labeled with `data_status: "SIMULATED_VOYAGE"` or `provenance: "SIMULATED"` in UI and API responses. |
| **16** | **What is actually live?** | (1) Live AIS queries to Open Waters API for sub-polar vessels within reachable bounding boxes; (2) Live COMNAP station coordinates verified against NCPOR reference benchmarks; (3) Live route generation executed on-demand per request. | [`services/ais_service.py:L260-L310`](file:///d:/SIH/backend/services/ais_service.py#L260-L310) | Satellite AIS coverage in pack ice has latency of 1–6 hours; real Antarctic operations rely on Iridium / INMARSAT satellite polling. |
| **17** | **What happens if satellite data is unavailable?** | Graceful degradation: The system falls back to climatological sea-ice baselines, persistence drift extrapolation, and records `provenance: "FALLBACK_CLIMATOLOGY"`. It **never** fabricates zero-ice conditions or converts data gaps into false "Low Risk". | [`optimization/polar_routing_engine.py:L270`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py#L270) | Climatological fallbacks have lower spatial fidelity than daily satellite passes and require wider safety margins. |
| **18** | **What happens if an iceberg appears suddenly?** | Dynamic obstacle insertion: The operator or radar detector calls `register_dynamic_iceberg()`. The KDTree spatial index immediately inserts the obstacle, recomputes CPA clearance margins, and triggers dynamic route re-optimization around the new hazard. | [`app/data_transformer.py:L401-L406`](file:///d:/SIH/backend/app/data_transformer.py#L401-L406) | Radar detection range is typically 12–24 nautical miles for tabular bergs and 3–6 NM for growlers/bergy bits. |
| **19** | **Can the system guarantee safety?** | **NO.** No navigation system can mathematically guarantee 100% safety in polar ice due to uncharted underwater growlers, rapid ice pressure ridges, and sudden blizzard whiteouts. The system provides **"lower modeled risk"** and **"risk-aware decision support"** based on available satellite and hydrodynamic models. | [`SIH_TECHNICAL_ARCHITECTURE.md`](file:///d:/SIH/SIH_TECHNICAL_ARCHITECTURE.md) | We explicitly reject terms like "100% safe" or "collision-proof". The master of the vessel retains ultimate operational command authority. |
| **20** | **What are the current limitations?** | (1) Grid resolution is 50 km (regional routing), not micro-tactical lead breaking (meter scale); (2) Monthly satellite CDR data limits rapid diurnal freeze-thaw cycles; (3) Vessel hydrodynamics use empirical Admiralty/Lindqvist models rather than full CFD; (4) SAR classification is tested on scene crops, not automated continent-wide ingestion. | [`SIH_FINAL_TECHNICAL_AUDIT.md`](file:///d:/SIH/SIH_FINAL_TECHNICAL_AUDIT.md) | These are standard trade-offs for a regional decision-support system operating within low-bandwidth shipboard compute limits. |
| **21** | **How would this scale to real Antarctic vessels?** | Containerized deployment on shipboard edge servers (e.g. ruggedized Linux box). Lightweight inference ($<50\text{ MB}$ RAM, $<50\text{ ms}$ CPU inference) requires no GPU. Low-bandwidth satellite sync (Iridium Certus: 50–100 KB compressed GRIB2 / NetCDF updates per day). | [`backend/requirements.txt`](file:///d:/SIH/backend/requirements.txt) | Shipboard installation requires integration with ECDIS (Electronic Chart Display and Information System) via NMEA 0183/2000 or RTZ route exchange format. |
| **22** | **How would you deploy this operationally?** | Dual-tier architecture: (1) **Shore-Side Operations Centre** (NCPOR Goa / AWI Bremerhaven) running continuous satellite ingestion, ML retraining, and fleet-wide monitoring; (2) **Shipboard Edge Node** running PolarRoutingEngine locally with cached rasters to maintain routing functionality when satellite links drop. | [`SIH_TECHNICAL_ARCHITECTURE.md`](file:///d:/SIH/SIH_TECHNICAL_ARCHITECTURE.md) | Operational certification would require formal compliance with IMO Polar Code (MSC.385(94)) and DNV/IACS ice navigation guidelines. |
| **23** | **How would you retrain the models?** | Automated monthly cron pipeline: Ingest new NSIDC CDR NetCDF rasters and BYU iceberg track updates, extract lag features, validate against recent observations using temporal train/test split, and atomically update serialized joblib artifacts with versioned metadata. | [`sea_ice/train.py:L215-L250`](file:///d:/SIH/backend/src/sea_ice/train.py#L215-L250)<br>[`iceberg/train.py:L180-L225`](file:///d:/SIH/backend/src/iceberg/train.py#L180-L225) | Retraining must strictly preserve chronological splitting (no future data leakage). Models should never be retrained during live API request threads. |
| **24** | **How would you validate the model in real operations?** | Compare forecasted SIC and iceberg positions against: (1) Shipboard bridge log observations and ice radar telemetry; (2) Next-pass Sentinel-1 SAR imagery; (3) High-resolution MODIS optical passes during cloud-free windows. Compute running MAE, displacement error (km), and CPA violation rates. | [`models/iceberg_metrics.json`](file:///d:/SIH/backend/models/iceberg_metrics.json)<br>[`models/sea_ice_metrics.json`](file:///d:/SIH/backend/models/sea_ice_metrics.json) | Continuous validation requires reliable ship-to-shore feedback channels transmitting daily bridge ice observation reports. |
| **25** | **What is innovative compared with normal route planning?** | (1) **True Coupling of AI Hazard Forecasters with Polar A\* Pathfinding** in EPSG:3031; (2) **Dynamic Time-Horizon Synchronization** (NOW, +6H, +12H, +24H, +48H) where icebergs drift and sea ice changes as the vessel transits; (3) **Decision Support with IMO POLARIS RIO Scoring**; (4) **Adversarial What-If Scenario Analysis** for pre-passage stress testing. | [`optimization/polar_routing_engine.py`](file:///d:/SIH/backend/src/optimization/polar_routing_engine.py)<br>[`app/server.py:L382-L440`](file:///d:/SIH/backend/app/server.py#L382-L440) | Commercial systems (e.g. SPOS, Transas) focus on calm-water Great Circle routing and weather routing; few integrate automated polar sea-ice lead optimization with iceberg CPA forecasting. |

---

*Compiled and verified against active executable code in repository `SIH-2026`.*
